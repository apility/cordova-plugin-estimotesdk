repositories{
  flatDir {
      dirs 'libs'
   }
}

dependencies {
   compile 'com.estimote:sdk:1.0.3:release@aar'
}

android {
  defaultConfig {
      minSdkVersion 16
      targetSdkVersion 25
  }
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}