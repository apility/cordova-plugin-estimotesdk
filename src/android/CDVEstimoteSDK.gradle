repositories{    
  jcenter()
  flatDir {
      dirs 'libs'
   }
}

dependencies {
   compile(name:'estimote-sdk', ext:'aar')
}

android {
  defaultConfig {
      minSdkVersion 16
      targetSdkVersion 22
  }
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}